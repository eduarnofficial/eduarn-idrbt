services:
  securebank-api:
    build: .
    container_name: securebank-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
